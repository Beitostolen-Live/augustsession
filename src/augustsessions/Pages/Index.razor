@page "/"

<PageTitle>August Sessions</PageTitle>
<HeadContent>
    <meta property="og:title" content="Beitostølen Live: August Session" />
    <meta property="og:url" content="https://www.augustsessions.no/" />
    <meta property="og:image" content="https://cdn.beitostolenlive.no/fb-share-image-countdown.png" />
</HeadContent>

@if(!IsActive)
{
    <CountDownTimer EndDate="@(new DateTime(2022,2,18,10,0,0))" />
}

@if(IsActive)
{
    <AugustSessionComponent />
}

@code {
    private bool IsActive = (new DateTime(2022,2,18,10,0,0)) <= DateTime.Now;
}
