@page "/"

<PageTitle>August Sessions // Beitostølen Live</PageTitle>
<HeadContent>
    <meta property="og:title" content="August Sessions // Beitostølen Live" />
    <meta property="og:url" content="https://www.augustsessions.no/" />
    <meta property="og:image" content="https://cdn.beitostolenlive.no/asfacbookheader.png" />
</HeadContent>

@if(!IsActive)
{
    <CountDownTimer EndDate="@(new DateTime(2022,2,18,10,0,0))" />
}

@if(IsActive)
{
    <AugustSessionComponent />
}

@code {
    private bool IsActive = (new DateTime(2022,2,18,10,0,0)) <= DateTime.Now;
}
